databaseChangeLog:
  - changeSet:
      id: 1
      author: rafaelcampos
      changes:
        - sql:
            # Comando SQL puro para criar o schema
            sql: CREATE SCHEMA IF NOT EXISTS auth;